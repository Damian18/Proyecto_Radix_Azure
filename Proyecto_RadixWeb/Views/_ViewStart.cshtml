
@if (User.IsInRole("Radix"))
{

    Layout = "~/Views/Shared/_LayoutRadix.cshtml";


}
else if (User.IsInRole("Administrador"))
{

    //Layout = "~/Views/Shared/_LayoutEmpresa_v2.cshtml";
    Layout = "~/Views/Shared/_LayoutSubEmpresa.cshtml";
}
else if (User.IsInRole("Administrador de sucursal") || User.IsInRole("Secretaria"))
{
    //Probando diferencia
    
    Layout = "~/Views/Shared/_LayoutSubEmpresa_v2.cshtml";

}
else
{


    Layout = "~/Views/Shared/_Layout.cshtml";

}
<script src="https://js.hs-scripts.com/4893307.js" type="text/javascript" id="hs-script-loader" async defer ></script>

@*<script>
        window.onload = function () {
            var contenedor = document.getElementById('contenedor_carga');
            contenedor.style.visibility = 'hidden';
            contenedor.style.opacity = '0';
        }

    </script>*@

@*<style>

        .lds-facebook {
            display: inline-block;
            height: 100px;
            width: 100px;
            border-radius: 100%;
            position: absolute;
            top: 50%;
            bottom: 50%;
            left: 50%;
            right: 50%;
        }

        #contenedor_carga {
            background-color: rgba(250,240,245,0.9);
            height: 100%;
            width: 100%;
            position: fixed;
            -webkit-transition: all 1s ease;
            -o-transition: all 1s ease;
            transition: all 1s ease;
            z-index: 10000;
        }

        .lds-facebook div {
            display: inline-block;
            position: absolute;
            left: 6px;
            width: 13px;
            background: green;
            animation: lds-facebook 1.2s cubic-bezier(0, 0.5, 0.5, 1) infinite;
        }

            .lds-facebook div:nth-child(1) {
                left: 6px;
                animation-delay: -0.24s;
            }

            .lds-facebook div:nth-child(2) {
                left: 26px;
                animation-delay: -0.12s;
            }

            .lds-facebook div:nth-child(3) {
                left: 45px;
                animation-delay: 0;
            }

        @@keyframes lds-facebook {
            0% {
                top: 6px;
                height: 51px;
            }

            50%, 100% {
                top: 19px;
                height: 26px;
            }
        }
    </style>*@
